{
  "name": "flashalliance-subgraph",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "set-addresses": "node scripts/set-addresses.js",
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --studio",
    "deploy": "graph deploy --studio flashalliance"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.79.0",
    "@graphprotocol/graph-ts": "^0.36.0"
  },
  "dependencies": {
    "assemblyscript": "^0.28.9"
  }
}
